interface:
  display_name: "Design Anchor"
  short_description: "Refactor existing UI/UX with token contracts"
  default_prompt: "Use $design-anchor to refactor existing B2B/admin product UI without rewriting business logic: establish or honor GenDesignSystem/Theme Lab or design-tokens.json for visual consistency, route interaction problems to the right UX prompt strategy, define AI-safe component boundaries before editing, borrow Vercel UI routes such as shadcn/v0/Geist/browser verification when useful, and reassemble pages with token-bound components or the existing component suite."
