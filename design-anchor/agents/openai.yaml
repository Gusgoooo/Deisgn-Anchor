interface:
  display_name: "Design Anchor"
  short_description: "B2B UI generation and governance"
  default_prompt: "Use $design-anchor to classify the project and request: generate a polished first B2B page from detailed or matched style prompts, or if the project is already mature, offer consent-gated existing-product governance with risk disclosure, read-only audit, governance plan, or explicitly confirmed first-stage changes. Keep UI work governed by tokens, @design components, specs, sync, MCP, and audit."
